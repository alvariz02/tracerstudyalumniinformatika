# TODO

- [ ] Fix `app/api/dashboard/route.ts` crash: define missing `calculateAnalytics` used by Dashboard API.
- [ ] Implement analytics computation in `calculateAnalytics(tracerData, angketData)`.
- [ ] Validate `GET /api/dashboard` returns 200 and does not throw `ReferenceError`.

